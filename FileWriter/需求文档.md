# 需求文档 — FileWriter 文件写入模块（异步日志/CSV/二进制记录）

> **定位**：基于 StreamBuffer + ThreadManage 的异步文件写入库。提供 printf 式格式化接口，攒批写盘（减磁盘磨损），支持日志/CSV/二进制，自动文件轮转（按数量+时间命名），优雅关闭。
> **平台**：IMX6ULL (ARM Linux) | **作者**：zlzksrl | **草案**：2026-07-10 | **最近更新**：2026-07-15
> **状态**：V1.2.0 已实现落地（本文档同步反映最新实现；设计决策见第五节；变更记录见第九节）

---

## 一、原始需求（用户提供）

1. 实现一个嵌入式代码用于记录日志、CSV 表格、二进制数据的库。
2. 使用 StreamBuffer 库 + ThreadManage 库实现。
3. 创建目录/多级路径/目标文件；获取当前写文件的不带路径的名字、绝对路径（含名字）、绝对路径（不含名字）。
4. 一定规则下同一类型创建的文件（如 `类型1_序号_时间.log`），可设最大文件数量，超过则删最老；也可无限制。
5. 获取当前写文件目录下的文件数量。
6. 写日志时可选每条数据前组合时间戳。
7. 写磁盘要收集一段数据后批量写入（减少频繁写操作）。
8. 写日志接口要有格式化输入功能（printf 式）。
9. 一个进程可能写多个文件，模块会被多处初始化多次，须**可重入**。
10. 停止记录时**优雅关闭**：阻止写入 → 缓存全刷盘 → 关闭文件 → 注销消费线程。
11. CSV 表头/各行内容由外部组织格式，库只管写（类似 printf 输出到文件）。

---

## 二、需求理解：异步文件写入管道

```
业务线程(多个)                         FileWriter 消费线程(每个实例1个)
   │ FileWriterAPI_Write(fmt, ...)       │
   │  → vsnprintf 格式化                  │
   │  → [可选] 加时间戳前缀                │
   │  → StreamBuffer.PutData() 入队       │
   ▼                                     ▼
              [StreamBuffer 攒批缓冲]
                     │  used≥阈值 / 超时 / 关闭
                     ▼
              FileWriter 消费线程
              Wait → GetData → fwrite → fflush
                     │
                     ▼
              磁盘文件（自动轮转）
```

- **生产者（业务线程）**：调 `Write(fmt, ...)` → 格式化 → 入 StreamBuffer，不阻塞、不等磁盘。
- **消费者（FileWriter 内置线程）**：等触发 → 批量取 → fwrite + fflush。
- **解耦**：业务不被磁盘 I/O 拖慢；攒批减少 fwrite 调用次数。

---

## 三、依赖库选择

| 库 | 角色 | 说明 |
|----|------|------|
| **StreamBuffer** | 攒批缓冲 | PutData 写入 → Wait/GetData 批量取出 fwrite（主要方案） |
| **ThreadManage** | 消费线程创建 | 用 ThreadAPI 创建消费线程，SCHED_RR 轮转策略，可配优先级 |

> FileWriter 依赖两个库：StreamBuffer（攒批）+ ThreadManage（线程管理）。
>
> **注**：早期草案曾计划用 MemoryPool 做格式化 buffer 池，实际实现评估后精简为**栈 buffer**（`char buf[FW_FORMAT_BUF_SIZE]`），理由：
> - Write 内 Alloc→vsnprintf→PutData(拷贝入 SB)→Free 全程同步完成，buffer 生命周期极短，无跨线程共享需求；
> - StreamBuffer 是拷贝式入队，PutData 返回后 buf 立即可复用/释放，栈变量完全等价；
> - 少一层依赖，减少 mutex 开销和代码复杂度。
> - 未来若扩展"结构化异步 API"（业务线程 Alloc buffer→塞给消费线程→线程消费后归还），再引入 MemoryPool。

---

## 四、功能规格

### 4.1 初始化 `FileWriterAPI_Init`
- 入参：配置结构体（见下）+ 实例句柄二级指针。
- 功能：
  - 创建目录/多级路径（`mkdir -p` 语义）。
  - 按命名规则创建第一个文件。
  - 初始化 StreamBuffer（容量/阈值/超时）。
  - 用 **ThreadManage ThreadAPI** 创建消费线程（SCHED_RR + 配置优先级）。
- **可重入**：每次 Init 创建独立实例（独立 StreamBuffer、独立文件句柄、独立线程），互不干扰。

### 4.2 配置结构体 `T_FileWriterConfig`
```c
typedef struct {
    /* 路径与命名 */
    char     dir_path[256];     /* 写入根目录（绝对或相对），自动创建多级 */
    char     date_subdir_prefix[16]; /* 日期子目录前缀，如 "X" → /log/X20260711/；空串=不按日期分目录 */
    char     file_prefix[64];   /* 文件名前缀，如 "sensor1" */
    char     file_ext[16];      /* 扩展名，如 ".log" / ".csv" / ".bin" */
    int      max_files;         /* 最大文件数量，合法范围 [0, 999]（越界 Init 拒绝返回 -1）：
                                 *   0        = 无限制，不做删除，也不遍历同前缀；
                                 *   1..999   = 有上限，超过时按文件名字典序(=时间序)删最老，
                                 *              Init 降配时批量删多余的；
                                 *   上限 999 与文件名 3 位 seq(0-999,%1000 循环)对齐，
                                 *   避免 wrap 后字典序与时间序错位、误删新文件。 */
    int      max_file_size;     /* 单文件最大字节，0=不限制；>0 则达此大小自动轮转。1G=1024*1024*1024 */
    int      auto_rotate_daily; /* 1=跨日自动轮转（建新日期目录+新文件）；0=不自动 */
    /* 线程 */
    int      thread_priority;  /* 消费线程 SCHED_RR 优先级（1~99，如 20）；0=用默认 */
    /* 写入行为 */
    int      timestamp;         /* 1=每行前加时间戳 "[HH:MM:SS.mmmmmm] "（不含年月日，文件名已带日期） */
    int      flush_bytes;       /* 攒批字节阈值（如 4096），达此值写盘 */
    int      flush_ms;          /* 定时写盘周期（如 100ms） */
    int      buffer_capacity;   /* StreamBuffer 容量（如 65536，须 2 的幂） */
} T_FileWriterConfig;
```

### 4.3 写入接口 `FileWriterAPI_Write` / `FileWriterAPI_WriteBin`
- **格式化写入**（printf 式）：
  ```c
  int FileWriterAPI_Write(T_FileWriter *fw, const char *fmt, ...);
  ```
  - 内部 `vsnprintf` 格式化到栈 buffer（`FW_FORMAT_BUF_SIZE`=1KB，单条超长自动截断）。
  - [可选] 前置时间戳（`timestamp==1` 时），格式 `[HH:MM:SS.mmmmmm] `。
  - 组合后 `StreamBuffer_PutData()` 入队（满则丢新，不阻塞业务）。
  - PutData 内部 memcpy 到环形缓冲，Write 返回后栈 buffer 立即回收。
- **二进制写入**：
  ```c
  int FileWriterAPI_WriteBin(T_FileWriter *fw, const void *data, int len);
  ```
  - 直接 PutData 原始字节（不加时间戳、不格式化）。

### 4.4 目录结构与文件命名

#### 4.4.1 路径组装与目录创建

**路径组装顺序**（从外到内）：
```
{dir_path}
  / {date_subdir_prefix}{YYYY_MM_DD}   ← date_subdir_prefix 非空时才有此层
  / {file_prefix 的路径部分}            ← file_prefix 含 "/" 时才有子目录
  / {file_prefix 的文件名部分}_{序号3位}_{YYYY-MM-DD-HH-MM-SS}.{ext}
```

**file_prefix 含路径分隔的示例**：
- `file_prefix = "sensor1"` → 文件直接在日期目录下：
  ```
  /log/X2026_07_11/sensor1_000_2026-07-11-12-41-30.log
  ```
- `file_prefix = "sensor/sensor1"` → 在日期目录下再建 `sensor/` 子目录：
  ```
  /log/X2026_07_11/sensor/sensor1_000_2026-07-11-12-41-30.csv
  ```

**目录创建逻辑**（`mkdir -p` 语义）：
1. 先组装完整绝对路径（拆分为目录部分 + 文件名部分）。
2. 逐级检查路径每一层是否存在（`access`/`stat`）。
3. 不存在的层依次 `mkdir`（从外到内）。
4. 最后 `fopen` 创建文件。

> 无论 `date_subdir_prefix` 是否为空、`file_prefix` 是否含 `/`，都走同一个"组装完整路径 → 逐级检查 → 逐级创建"流程，统一处理。

#### 4.4.2 文件命名规则
- 格式：`{prefix}_{序号3位}_{YYYY-MM-DD-HH-MM-SS}.{ext}`
  - 例：`sensor1_000_2026-07-11-12-41-30.csv`
  - 序号 3 位零填充（000, 001, ..., 999），wrap 后回到 000；同名冲突由文件名尾部秒级时间戳承担唯一性。
- 完整路径示例（日期子目录开启）：
  ```
  /log/X2026_07_11/sensor1_001_2026-07-11-12-00-00.log
  ```

#### 4.4.3 文件轮转
- **序号递增（V1.2）**：每次 rotate `file_seq = (file_seq + 1) % 1000`，回绕到 000 后由文件名尾部秒级时间戳承担唯一性；seq 只做人眼可读的递增编号。
- **跨重启延续（V1.2）**：Init 时扫描 `current_dirpath` 下所有 `{prefix_name}_NNN_*` 文件，取最大 3 位序号 `max_seq`，`file_seq = (max_seq + 1) % 1000`。避免重启后从 000 起冲撞已存在的旧文件。`max_files=0` / `1..999` 两种模式一致地都做这次扫描。
- **最大数量限制**：
  - `max_files == 0`：无限制，Init/rotate 均不做删除、也不做同前缀遍历（`0` 的语义就是"完全不管，跳过所有清理相关的目录 I/O"）；仅 Init 扫 `max_seq` 用于跨重启延续。
  - `max_files ∈ [1, 999]`：Init 结束时 + 每次 rotate 后各做一次清理。匹配 `{file_prefix的文件名部分}_*` 计数，超过则单次遍历 + `qsort` 字典序升序 + 批量 `remove` 掉最老的 `excess = count + 1 - max_files` 个。不同前缀的文件（如 `other_*.csv`）**不计入**。**降配启动**（如上次 999→本次 100）在 Init 阶段一次性批量删多余的，替代 V1.1 早期 O(N²) 循环删（20-60s → 1-3s）。
  - **`< 0` 或 `> 999`**：Init 拒绝返回 -1。上限 999 与 seq wrap 边界对齐——超过 999 后字典序会与时间序错位、误删新文件。
- **触发轮转的场景**：
  - Init 时创建第一个文件（此时读取 max_seq 后 +1 起编号）；
  - 手动 `Rotate()` 创建新文件；
  - 消费线程检测到文件大小达 `max_file_size` 自动轮转；
  - 消费线程检测到跨日（`auto_rotate_daily=1` 时）自动轮转。

### 4.5 优雅关闭 `FileWriterAPI_Destroy`（一步到位）
> 实现上没有单独的 `Close`，Destroy 内部包含关闭 + 释放。业务侧唯一入口就是 Destroy。

```
FileWriterAPI_Destroy(&fw)
   → Phase A（同步）：
       destroying=1（阻止新的保护区进入）
       → StreamBuffer.Close（阻止新入队）
       → Flush 唤醒消费线程
       → 消费线程排空 SB (Wait→GetData→fwrite 直到 CLOSE_EMPTY)
       → 消费线程 fflush + fclose，数据完整落盘
       → pthread_join 消费线程
       → spin-wait 至 ref_count=0（超时 destroy_wait_ms，默认 500ms）
       → CAS 抢释放权成功 → fw_final_free（释放 SB + 结构体）→ *pp=NULL
   → Phase B（延迟，仅 Phase A 超时未归零时进入）：
       destroy_pending=1 → *pp=NULL（用户视角句柄已归还）
       → 最后一个 LEAVE 的 Writer 检测到 destroy_pending
         → CAS 抢释放权 → 自行 fw_final_free
```
- Destroy 返回时 Phase A 已完成前 4 步，**数据保证完整落盘**。
- 任何情况下 Destroy **有界返回**（最长 `destroy_wait_ms` + 消费线程 drain 时长）。
- Phase B 极端场景（业务线程永挂）实例内存延迟释放（约几 KB），仍保证不 UAF、不 double-free。

### 4.6 查询接口
| 函数 | 说明 |
|------|------|
| `GetCurrentFileName` | 当前写文件不带路径的名字（如 `sensor1_000_2026-07-11-12-41-30.csv`） |
| `GetCurrentFilePath` | 当前写文件绝对路径（含名字，如 `/log/X2026_07_11/sensor1_000_2026-07-11-12-41-30.csv`） |
| `GetCurrentDirPath` | 当前写文件绝对路径（不含名字，如 `/log/X2026_07_11`） |
| `GetFileCount` | 当前日期目录下同前缀的文件数量 |

### 4.7 销毁 `FileWriterAPI_Destroy`
- 见 4.5——Destroy 是唯一的关闭入口，无独立 Close 接口。
- V1.1 起支持**抗并发销毁**：业务线程持有句柄期间另一线程调 Destroy 也安全。

---

## 五、关键设计决策（已实现落地）

### D1. 缓冲方案
- **StreamBuffer** ✓：字节流攒批，PutData(格式化后的字符串) → GetData → fwrite。

### D2. 消费线程
- **库内置消费线程** ✓：用 **ThreadManage 的 ThreadAPI** 创建（非裸 pthread_create）。
- **SCHED_RR 轮转策略**：消费线程使用 `SCHED_RR` 实时调度策略（不同进程中写盘线程优先级可能不同，需精确控制）。
- **可配优先级**：Init 时通过 `thread_priority` 字段配置（1~99，SCHED_RR 优先级范围）。
  - 典型值：日志/非关键文件写盘线程可设较低优先级（如 10~30），不抢占业务线程。
  - 不同进程的 FileWriter 实例可设不同优先级。

### D3. 文件轮转触发（初始化配置）
- **文件最大大小**：`max_file_size` 配置（字节），0=不限制；>0 时文件达到该大小自动轮转创建新文件。
- **跨日期自动轮转**：`auto_rotate_daily` 开关，1=跨日自动新建文件（配合日期子目录）。
- **手动 Rotate()**：随时可手动触发。
- 后续可扩展：定时轮转。

### D3.5 日期子目录
- `date_subdir_prefix` **非空**时按日期创建子目录（`{base}/{prefix}{YYYYMMDD}/`）。
- 消费线程每条写之前检查日期是否变化，变化则自动切目录+新建文件（跨日无缝）。
- `date_subdir_prefix` **为空串**时不分日期目录，所有文件直接在 `base_dir` 下。

### D4. 格式化 buffer 来源
- **栈 buffer** ✓（`char buf[FW_FORMAT_BUF_SIZE]`，1KB）：Write 函数栈上分配，vsnprintf 格式化后 memcpy 入 StreamBuffer，Write 返回时栈自动回收。
- 早期草案曾用 MemoryPool，评估后发现 Alloc/Free 全程同步且 buffer 无跨线程共享，池化无实际收益，遂改用栈变量。详见第三节"依赖库选择"注释。
- 单条日志 >1KB 会被截断（vsnprintf 返回值检查），不越界。

### D5. 时间戳格式
- **`[HH:MM:SS.mmmmmm] `**（含微秒，不含年月日——文件名已有日期）。
- 示例：`[16:50:44.789550] `
- 由 `timestamp` 开关控制（1=加前缀，0=不加）。

### D6. 库名 / 前缀
- **FileWriter**（`FileWriterAPI_*`）✓

---

## 六、公开 API 草案

```c
typedef struct T_FILEWRITER T_FileWriter;

int  FileWriterAPI_Init(T_FileWriter **fw, const T_FileWriterConfig *cfg);
int  FileWriterAPI_Destroy(T_FileWriter **fw);

int  FileWriterAPI_Write(T_FileWriter *fw, const char *fmt, ...);    /* printf 式格式化 */
int  FileWriterAPI_WriteBin(T_FileWriter *fw, const void *data, int len);  /* 二进制 */

int  FileWriterAPI_Flush(T_FileWriter *fw);     /* 立即触发一次写盘（异步） */
int  FileWriterAPI_Rotate(T_FileWriter *fw);    /* 手动轮转（创建新文件） */

/* 查询 */
int  FileWriterAPI_GetCurrentFileName(T_FileWriter *fw, char *out, int out_len);
int  FileWriterAPI_GetCurrentFilePath(T_FileWriter *fw, char *out, int out_len);
int  FileWriterAPI_GetCurrentDirPath (T_FileWriter *fw, char *out, int out_len);
int  FileWriterAPI_GetFileCount      (T_FileWriter *fw);   /* 同前缀 */
int  FileWriterAPI_GetTotalFileCount (T_FileWriter *fw);   /* 目录下所有 */
int  FileWriterAPI_StatsGet          (T_FileWriter *fw, T_FileWriterStats *out);

/* 工具函数（不依赖实例） */
int  FileWriterAPI_GetTimeString(char *out, int out_len, const char *fmt);
int  FileWriterAPI_MakeDirs     (const char *path);
```

> 注：实现上没有独立 `FileWriterAPI_Close` 接口，关闭与销毁合并到 `FileWriterAPI_Destroy` 一步到位。

---

## 七、典型应用

- **异步日志**：`FileWriterAPI_Write(fw, "[moduleA] ret=%d\n", ret);` → 自动加时间戳 → 攒批写盘。
- **CSV 记录**：外部组织行内容 → `FileWriterAPI_Write(fw, "%d,%d,%.3f\n", ts, ch, val);`。
- **二进制 bin**：`FileWriterAPI_WriteBin(fw, &frame, sizeof(frame));` → 攒批写盘。
- **多实例**：一个进程 Init 多个 FileWriter（不同目录/前缀），各自独立线程写盘。

---

## 八、项目结构（已落地）

```
FileWriter/
├── include/FileWriter.h              # 公共 API（opaque pointer + extern "C" + Doxygen）
├── src/
│   ├── FileWriter_Main.h             # 内部结构体 T_FILEWRITER + 宏 + 并发访问约定
│   ├── FileWriter.c                  # 核心实现（StreamBuffer 攒批 + ThreadManage 消费线程 + 抗并发销毁）
│   └── FileWriter_Maketime.h         # Makefile 生成的版本时间戳（不手写）
├── debug/
│   ├── main.c                        # 7 段功能演示：LOG/CSV/BIN/多实例/Rotate/查询/工具
│   ├── stress_test.c                 # 抗并发销毁高压测试（V1.1）+ Stage 4 功能断言（V1.2）
│   ├── Makefile                      # 交叉编译（arm-linux-gnueabihf-gcc）+ 链接依赖库
│   └── ProjectInfo.txt               # 编译产物 MD5 记录（Makefile 生成）
├── 需求文档.md                        # 本文件（原始需求与设计决策）
├── AI审查结果.md                      # 六轮代码审查记录（V1.0-V1.2）
└── readme.md                         # 使用说明与 FAQ
```

隶属 `DataStructureLibrary` 仓库，复用其 git，风格与 StreamBuffer / ThreadManage 对齐。

**依赖库路径**（公共库目录 `DataStructureLibrary/Library/`）：
```
Library/
├── StreamBuffer/include/StreamBuffer.h       + StaticLib/libStreamBuffer.a
└── ThreadManage/include/ThreadManage.h       + StaticLib/libThreadManage.a
```
Makefile 从此路径取头文件（`-I`）和静态库（`-L` + `-l`），路径由 `LIBFILE_PATH` 变量控制（CI/环境可 override）。

**编译器要求**：V1.1 起依赖 `<stdatomic.h>`（C11），Makefile 显式 `-std=gnu11`；GCC 4.9+ 支持。

---

## 九、变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| V1.0.0 | 2026-07-11 | 首版发布：核心 API + 事务性 Rotate（先 fopen 新 → 后 fclose 旧）+ drain-before-rotate + Stats API |
| V1.1.0 | 2026-07-12 | **抗并发销毁**：atomic 引用计数 + CAS 独占释放权 + 两阶段销毁（Phase A 同步 / Phase B 延迟）；跨日 rotate 失败可自愈；新增配置字段 `destroy_wait_ms`；`debug/stress_test.c` 高压测试；`-std=gnu11`（依赖 `<stdatomic.h>`） |
| V1.2.0 | 2026-07-15 | **序号跨重启延续 + 降配批量清理**：Init 扫描同前缀 `max_seq`，`file_seq = (max_seq+1) % 1000`，避免重启后 seq 从 000 起冲撞已存在旧文件；Rotate 增量改为 `%1000`（seq 只在 000-999 循环，重号靠文件名秒级时间戳区分）；`max_files` 范围校验 `[0, 999]`，越界 Init 拒绝；降配启动（如上次 999 → 本次 100）用"单次遍历 + qsort + 批量 remove"替代 V1.1 早期 O(N²) 循环删（800 个文件 20-60 秒 → 1-3 秒）；`stress_test.c` 新增 Stage 4 功能断言（参数校验 / seq wrap / 跨重启延续 / 降配批量清理） |

### 抗并发销毁机制补充说明（V1.1 落地）

四个 atomic 字段协作：

| 字段 | 语义 |
|---|---|
| `ref_count` | 保护区引用计数。Write/Rotate/查询接口入保护区时 +1，出保护区时 -1 |
| `destroying` | Destroy 已开始的标志。置位后新的 `FW_ENTER_GUARD` 直接失败（Write 返回 -2） |
| `destroy_pending` | Phase B 触发标志。Phase A 超时未归零时置位，通知业务线程"最后一个 LEAVE 负责释放" |
| `finalize_taken` | 释放权 CAS(0→1) 独占锁，保证 `fw_final_free` 只调一次（Destroy 主线程 / 业务线程二选一） |

**Phase A（同步阶段，Destroy 内完成）**：置 destroying → SB.Close → Flush 唤醒消费线程 → join 消费线程 → **数据完整落盘** → spin-wait ref_count 归零（超时 `destroy_wait_ms`）→ CAS 抢释放权 → `fw_final_free`。

**Phase B（延迟释放，Phase A 超时未归零时进入）**：置 destroy_pending → `*pp=NULL` 归还给用户 → 业务线程最后一个 LEAVE 时通过 CAS 抢到释放权 → 自行 `fw_final_free`。

**极限保证**：任何情况下 Destroy 有界返回（最长 `destroy_wait_ms` + 消费线程 drain），文件数据一定完整落盘；Phase B 极端场景（业务线程永挂）内存延迟释放约几 KB，仍保证不 UAF、不 double-free。

---

## 十、后续迭代（V1.3 计划）

- `FileWriterAPI_StatsReset` — 清零累计计数器
- `FileWriterAPI_FlushSync(fw, timeout_ms)` — 同步等待落盘
- `T_FileWriterConfig.fsync_ms` — 周期性 fsync 到 flash
- `T_FileWriterConfig.format_buf_size` — 单条日志上限可配
- `FileWriterAPI_SetLogger(log_fn)` — 库内日志重定向到用户回调
- `T_FileWriterStats.destroy_deferred` — Phase B 触发计数暴露给用户
